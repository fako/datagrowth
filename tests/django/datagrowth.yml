mock_processor:
  include_odd: false
  include_even: false

shell_resource:
  bin_dir: /project/bin

micro_service:
  connections:
    mock_service:
      protocol: "http"
      host: "localhost:2000"
      path: "/service/"
